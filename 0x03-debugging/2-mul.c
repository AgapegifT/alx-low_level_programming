#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: the first number of multiply
 * @y: the second number of multiply by first number
 * Return: the result of multiplication between x param and y param
 */

int mul(int x, int y)
{
	return (x * y);
}
