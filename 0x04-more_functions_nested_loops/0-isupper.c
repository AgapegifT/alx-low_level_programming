#include "main.h"

/** _isupper - check check if a letter is upper case
 * @x: The Number to be checked
 * Return: 1 for upper letter or 0 for else
 */

int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
	return (0)
}
